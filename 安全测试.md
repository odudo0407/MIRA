# Mira 安全测试操作指南（part2）

> 本文档依据 `Mira_AI评测与安全测试实操指南.md` 与 `Mira质量评测总报告.md` §3 整理。9 条用例、实测结果、发现问题均来自实际执行，未做推断或补充。

## 1. 概述

| 项 | 内容 |
|---|---|
| 测试范围 | 认证接口、管理员接口、面试接口 |
| 测试维度 | SQL 注入 / XSS / 越权 / Token 篡改 / 未授权访问 / 异常参数 |
| 用例数 | 9 条（S01-S09） |
| 执行结果 | 7 通过 / 2 发现问题（SEC-01 nickname XSS、SEC-02 密码超长） |
| 被测系统 | Mira 智能模拟面试平台（Flask，端口 8000） |

> 说明：实际执行中 SQL 注入、Token 防篡改、权限隔离、未授权拦截、异常参数处理 **均通过**，仅发现 2 项需改进问题，无 0.0.0.0 端口暴露类问题记录。

## 2. 环境准备

```bash
# backend 起着（端口 8000）
# 注册测试账号并取 token（见质量测试指南 §2）
# 测试用变量（Windows cmd）：
set TOKEN=<你的token>
```

## 3. 安全用例总表（实际执行结果）

| 编号 | 类型 | 接口 | 操作 | 预期 | 实测 | 结果 |
|---|---|---|---|---|---|---|
| S01 | SQL 注入 | /api/auth/register | email 注入 `' OR '1'='1` | 400 拒绝 | 参数化查询拦截 | ✅ 通过 |
| S02 | SQL 注入 | /api/auth/login | email 注入 `' OR '1'='1'--` | 400 拒绝 | 参数化查询拦截 | ✅ 通过 |
| S03 | XSS | /api/auth/register | nickname=`<script>alert('XSS')</script>` | 转义存储 | 原样存储并返回 | ❌ **SEC-01** |
| S04 | 越权 | /admin/overview | 普通用户带 Token 访问 | 403 | 403 | ✅ 通过 |
| S05 | 未授权 | /api/interview/history | 不带 Token 访问 | 401 | 401 | ✅ 通过 |
| S06 | Token 篡改 | /api/auth/me | 伪造 / 篡改 Token | 401 | 401（itsdangerous 拒绝） | ✅ 通过 |
| S07 | 空 Token | /api/auth/me | `Bearer ` 空值 | 401 | 401 | ✅ 通过 |
| S08 | 异常参数 | /api/interview/chat | messages 为空 `[]` | 400 | 400（非 500） | ✅ 通过 |
| S09 | 超长密码 | /api/auth/register | password 传 200 字符 | 400 拒绝 | 200 注册成功 | ❌ **SEC-02** |

> 防御有效项（7 项）：SQL 注入防护、Token 签名防篡改、权限隔离（`@require_admin` 返 403）、未授权访问拦截（`@require_auth` 返 401）、异常参数处理（返 400 而非 500，无信息泄露）。

## 4. 关键测试实操（真实命令）

### S03 存储型 XSS 隐患（SEC-01）
```bash
curl -s -w "\nHTTP状态码: %{http_code}\n" -X POST http://127.0.0.1:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"xss@test.com\",\"password\":\"Test123456\",\"nickname\":\"<script>alert('XSS')</script>\"}"
# 注册返回 200，再用 /me 验证 nickname 是否被转义：
curl http://127.0.0.1:8000/api/auth/me -H "Authorization: Bearer %TOKEN%"
# 实测 nickname 原样返回 <script>alert('XSS')</script> → API 层存在存储型 XSS 隐患
# （前端为 React 默认转义，但 API 层应做纵深防御）
```

### S04 越权访问（通过）
```bash
curl -s -w "\nHTTP状态码: %{http_code}\n" http://127.0.0.1:8000/admin/overview \
  -H "Authorization: Bearer %TOKEN%"
# 预期 403，实测 403 ✅
```

### S06 Token 篡改（通过）
```bash
curl -s -w "\nHTTP状态码: %{http_code}\n" http://127.0.0.1:8000/api/auth/me \
  -H "Authorization: Bearer FAKE_TOKEN_12345"
# 预期 401，实测 401 ✅
```

### S09 超长密码（SEC-02）
```bash
curl -s -w "\nHTTP状态码: %{http_code}\n" -X POST http://127.0.0.1:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"longpwd@test.com\",\"password\":\"<200字符的a>\",\"nickname\":\"超长密码\"}"
# 预期 400（密码超长应拒绝），实测 200 → 无最大长度限制（Bug-002）
```

## 5. 发现的问题

| 编号 | 缺陷 | 严重等级 | 状态 |
|---|---|---|---|
| SEC-01 | nickname 字段未做 HTML 转义，`<script>` 原样存储并返回，API 层存在存储型 XSS 隐患 | 一般 | 已记录，输出修复建议 |
| SEC-02 | 密码无最大长度限制，仅校验 ≥6 位，200 字符超长密码可注册成功 | 轻微 | 已记录，输出修复建议（即 Bug-002） |

## 6. 修复建议

- **SEC-01**：nickname 入库前 / 输出时做 HTML 转义（`<` → `&lt;`）；前端已默认转义，API 层补纵深防御。
- **SEC-02**：注册接口增加 `len(password) > 128` 校验，闭环成本极低（即 Bug-002 修复）。

## 7. 补充提示

- SQL 注入测试安全：SQLAlchemy 使用参数化查询，注入 payload 会被当普通字符串处理，不会执行恶意 SQL，不会破坏数据库。
- 测试数据清理：可在 phpMyAdmin 中 `TRUNCATE user_accounts` 清掉测试账号。
